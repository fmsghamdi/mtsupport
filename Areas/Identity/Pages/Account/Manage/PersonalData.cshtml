@page
@model PersonalDataModel
@{
    ViewData["Title"] = "البيانات الشخصية";
    Layout = "_ManageLayout";
}

<div class="card shadow-sm border-0">
    <div class="card-header bg-primary text-white">
        <h5 class="mb-0">
            <i class="bi bi-database me-2"></i>البيانات الشخصية
        </h5>
        <small class="opacity-75">إدارة وتحميل بياناتك الشخصية</small>
    </div>
    
    <div class="card-body p-4">
        <p class="text-muted mb-4">
            يمكنك تحميل أو حذف بياناتك الشخصية من هذه الصفحة
        </p>
        
        <div class="alert alert-danger mb-4">
            <i class="bi bi-exclamation-triangle me-2"></i>
            <strong>تحذير:</strong> حذف البيانات لا يمكن التراجع عنه!
        </div>
        
        <div class="d-flex gap-3">
            <form asp-page-handler="DownloadPersonalData" method="post">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-download me-2"></i>تحميل البيانات
                </button>
            </form>
            
            <a asp-page="./DeletePersonalData" class="btn btn-danger">
                <i class="bi bi-trash me-2"></i>حذف البيانات
            </a>
        </div>
    </div>
</div>
